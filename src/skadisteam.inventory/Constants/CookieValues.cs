namespace skadisteam.inventory.Constants
{
    /// <summary>
    /// Class which includes all cookie values for the package.
    /// </summary>
    internal static class CookieValues
    {
        /// <summary>
        /// Value for the Steam language.
        /// This is always english so we can parse the same
        /// results all the time.
        /// </summary>
        internal const string SteamLanguage = "english";
    }
}
